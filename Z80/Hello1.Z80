SPRINT	POP HL
	LD A,(HL)
	INC HL
	PUSH HL
	CP $FF
	RET Z
	PUSH AF 	;<- OLD ROM ONLY
	CALL PRPOS	;<- OLD ROM ONLY
	POP AF		;<- OLD ROM ONLY
	CALL PRINT
	JR SPRINT
	CALL SPRINT
	DEFM "HELLO WORLD"
	DEFB $FF
	RET
